---
name: vision-recognition-traps
tier: T2  # T分级: T2=直接做 / T1=先请示 / T0=一律拒
description: 用视觉模型识图前先读——问法诱导/文字自动纠错/发色色准/角色点名/场景归属脑补等陷阱与对策。触发：识图、看图、图片识别、截图分析、解图题、AI 生成图质检（「总觉得哪里不对劲」）。
---

# 视觉模型识别陷阱

> 用 mmx vision / ds-vision / vision_analyze 识图时的通用坑。2026-08-06 表情包批量建档 10+ 张实战总结（大量被阁下纠正后沉淀）。
> **陷阱编号 1-19 是稳定锚点**——别的 skill 按编号引用（陷阱 1/3/5/6 最常被引），改表别改号。
>
> **读者须知（路径与工具）**：下文 `/opt/data` 是作者环境的仓库根，读者按自己路径替换；`mmx` / `ds-vision` / `vprompt` 是作者环境的视觉入口——**陷阱与判据部分与工具无关**，换任何视觉模型都成立，把自己的调用替换进去即可。`internet-memes-reference` / `twin-vision-archive` 是作者的私人资料库 skill，读者可忽略。

## 第一步：选入口

| 要做什么 | 入口 |
|:---|:---|
| 默认识图 | `mmx vision describe <图>`——第一入口不是备选；工具不在会话列表里就调 CLI，**别先说不看图** |
| mmx 报错 / 内容安全拒绝 | 切 `bin/ds-vision <图> "<问题>"` 兜底，流程照走，别停下等修复 |
| 要凑近看（生僻字/小字/色号/细节） | `vision_analyze(image_url=<路径|URL>, question=..., region=[x1,y1,x2,y2])`（region 是**原图像素坐标**） |
| gif / 客观描述 / 批量扫图 / 成本敏感 | `bin/ds-vision`：`--desc`（默认问法）/ `--batch`（批量）/ `--v4` `--meme`（专用提示词） |
| 界面截图 / 表格读字 | 走**真 OCR**：`vtrap ocr <图> --json out.json`（裁块复核 `--crop x1,y1,x2,y2 --scale 6 --read`）→ `references/ui-ocr-workflow.md` |
| 两图差分（水印／改图取证） | `vtrap diff A.jpg B.jpg` → `references/image-diff-forensics.md` |

`vtrap` 是本 skill 的统一入口（`ocr` / `diff` 两个子命令，自动挑带依赖的解释器）；两个子命令也照旧可单跑 `scripts/ui_ocr.py` / `scripts/imgdiff.py`。
QQ 收图与 md5 验原图、批量提示词体系、ds-vision 的选项坑 → `references/entrypoints.md`

## 三条心法

1. **重心永远在画面层，先答「这是什么」**——内容/场景是每次都要答的；文件层（EXIF／块结构／尾拼／LSB）只在图有蹊跷时才动。上来就 probe 隐写是尴尬的条件反射（文件层工具 → `credential-management/references/steganography.md`）。
2. **模型输出是「它认为的」，不是图上事实**——文字、颜色、身份、**数量与视角**全部实测翻车过；拿不准的按陷阱 1/3 的铁律问人。
3. **「两张图是不是同一张」不靠描述判**——只认硬字段（像素尺寸／时间戳／md5／dhash）；找回原档用 dhash（8×8）扫库，别用 md5 单比（转发/导出会重编码，字节数变、尺寸不变）。→ `references/image-diff-forensics.md`

## 陷阱速查（19 条）

| # | 陷阱 | 一句话对策 |
|:--|:-----|:-----------|
| 1 | **文字「自动纠错」**——读出来 ≠ 图上写的 | 关键梗字拿不准就问人；低分辨率图放大 8 倍 LANCZOS 终审；生僻字以文本层为准；弧形文字不采信 vision。**水印与署名要拆开**——VLM 把「豆包AI生成」读成「百度AI生成」已实测两次，**平台水印**只判「有/无」、品牌不引述（以阁下确认为准）；而**作者自己加的手写签名/署名不是水印**（生成提示词里常常明写，如「左下角添加手写签名」），它是逐字可查的定位线索，要单独点出来、别当水印丢掉 |
| 2 | **发色/颜色读数不可靠** | 颜色只当线索，别做排除法；用萌娘词条发色/瞳色双字段核对 |
| 3 | **角色点名九成错** | 人（阁下点名）> 自家档案 > 搜索交叉验证 > 模型点名；logo 元素是硬线索。**打底给名字 ≠ 安全**（2026-09-10 实测：`ds-vision --desc` 打底「冬装阿尼娅/高奈利亚/苏小真/伊珂丝」四张全错——被配到《明日方舟》《偶像大师》《女神异闻录5》《魔法纪录》，且措辞肯定）——提示词必须**明文禁止作品归属推断**，否则模型会“给名字找个作品”而不是标未知 |
| 4 | **拼贴截图漏结构+用途** | prompt 显式问结构/水印/字幕；用途等上下文确认再归档 |
| 5 | **平台/UI 识别错**（贴吧→小红书） | 平台名以图上文字为准；归档前拿不准问阁下 |
| 6 | **内容归属/视角判断错** | 主语线索判阵营（我们=自己人/你们=对方）；语气判立场 |
| 7 | **官方恶搞图**（愚人节） | 「图真实存在」与「内容为假」分开标注 |
| 8 | **小字/代号脑补成厂商名** | 裁剪放大 2-4 倍重识；角色归属直接问当事人；当事人没把握=不收录 |
| 9 | **推理模型 reasoning 吃掉输出** | 视觉任务 max_tokens ≥ 4000；v4/meme 提示词 12000 起步 |
| 10 | **游戏/场景归属脑补**——模型对「这是哪个游戏/地区/任务」的判断会编，同图两次识别给不同答案（2026-08-28 鸭嘴兽生命周期图：一次「须弥活动」一次「纳塔任务」，都不对） | 归属只当线索不当结论；**先读题面/底部提示文字定位这图要干嘛**，出处以图内文字+阁下确认为准 |
| 11 | **公众号聚合/列表视图混两篇**——账号历史消息/列表截图里 banner 标题与下方内容常是两篇不同文章，vision 会读成「一篇内容怪异的文章」（2026-09-04：法院号普法文 banner + 下方执行案款公示被当成同一篇） | 先辨结构：标题条与正文归属可能不同；**以 mp.weixin 原文提取文本为准校对**（wechat-article skill），vision 读文只当线索 |
| 12 | **专有名词被「正字法化」——拆词/加空格/自补大小写**（2026-09-10：Steam 官方名 `RingTower: Girls' Rhapsody` 被读成「Ring Tower」多一个空格；同图还把副标题当成并列的第二标题） | 作品/公司/品牌英文名以**商店页、官网字段原文**为准，模型读音只当线索；建档/入库前核一次原文 |
| 13 | **品牌 logo 被读成近似物**——「logo 是硬线索」不等于模型读得对（2026-09-10：DeepSeek App 截图，mmx 把蓝色鲸鱼 logo 读成「火焰/水滴形状的图标」，ds-vision 读对为鲸鱼） | 自家/常见品牌 logo 优先用**官方视觉模型（ds-vision）**复核，或直接问阁下；logo 认错会连带把整张图的性质判错 |
| 14 | **诱导性问法制造幻觉瑕疵**（2026-09-10 阁下纠正）——把「瑕疵/问题/崩/坏/穿模/错」写进 prompt，模型必照单交作业，且顺着你给的字眼（粘连/穿模/崩坏）往重里说；同一张图换中性问法，重话全消失（实测：AI 生成角色图，问「检查有没有瑕疵」读出「严重崩坏、手指嵌进碟子、肉疙瘩」，改问中性「描述双手位置和动作」只剩「模糊、边界不清」） | **先问「这是什么」再谈其余**——识图先答这是什么，别先挑刺；默认用客观描述问法（`ds-vision --desc`）；挑毛病必须给中性词表，把「问出来的」与「看到的」分开定性；瑕疵结论一律**另开一轮中性问法复核**才算数（判据见通用对策 6「同模型换问法自检」）；诱导式问法产出的负面结论不进档案 |
| 15 | **手写体/艺术字/被遮挡文字被「按语境补全」**——多轮裁切读数互相矛盾还越读越「合理」（2026-09-11 生日贺图：手写英文名三版同读 `Kasutaria`、官方罗马字实为 `Castalia`；左侧清单右半被大标题压住，只读得到 `cak…`/`gif…`/`ca…`，两版分别补齐成 `cake/gift/card` 与 `cake/gift/card/balloons`，勾选态也来回翻） | **只录可见字母 + 标「[遮挡]」**，禁按语境补全（生日贴纸→cake/gift/card 就是补全）；细节拿不准**照实标存疑、写明「以原图为准」**，宁缺不圆（勾选态看不清就不录）；手写体专名与官方档冲突时**两版并列留痕**，别用「更合理的那个」盖掉图面读数 |
| 16 | **图根本没取到，却对着报错/占位图下结论**——甩来的是 CDN/镜像 URL，站点风控或原图已删时 `vision_analyze` 报 403，报错 URL 里是 `…/images/default_h_large.gif`（图床的「图片不存在」占位图）。此时正确结论是**「这张图在源站不存在」**，不是「图有问题」也不是「看不了图」（2026-09-11 微博图床实测：同一博主前缀、时间码相邻的两张，前一张 200 真图、后一张 302 占位） | **URL 识图前先验直链存活**：`curl -sIL -H "Referer: <站>" -H "User-Agent: Mozilla/5.0" <原图床直链> \| grep -Ei "^(HTTP\|content-type\|content-length\|location)"`——真图 `200 image/jpeg` + 正常体积；`302 → default_h_large.gif` 判死。**镜像/中转站 403 ≠ 图没了**（站级风控连好图一起拒），要回原图床验，多域名/多尺寸交叉（详见 `news-verification` 模块 C+「图片直链存活校验」）；死图照实报「源站已删/链接不对」，**绝不描述一张没取到的画面** |
| 17 | **一行小图标/平台 logo 条读出互斥答案**——图标小、字形近似（Switch 双手柄／PS 的 P+S／Xbox 的 X），模型靠整体印象填，同一张图三问三答（同张预告海报的平台标：PS·Switch·Xbox ／只有一个 Xbox ／ Steam·PS·Switch·Xbox）。**同类还包括右下角小字徽章：CERO 分级徽章被读成「CERO B」，逐字问实为「CERO 審査予定」（2026-09-17 合金弹头 TGS 幻灯片）**；**同一张图两轮不同问题还会给出不同平台集合**（同一张 MSUC 预告图：一轮列 Switch2/Switch/PS5/XSX/Steam，另一轮只列 Switch/XSX/Steam——**必须以「一次问清一共几个、从左到右分别是什么」的那一轮为准**） | **裁到图标条 + 放大 2~4 倍再问，且一次问清「一共几个、从左到右分别是什么」**——别在同一张整图上追问，追问只会在几个错答之间来回翻。这类图标条只当「目标平台声明」的线索；落到实处（到底上架没有）必须去各平台商店实测，见 `news-verification` 模块 B+ |
| 18 | **AI 生成图「总觉得哪里不对劲」——不是画坏了，是「没有物理」**：内容描述能满分通过（模型还夸「精美、高质量」），但部件之间没有接触、没有投影、遮挡顺序错乱（背景的边线在羽毛/发丝处断掉）、装饰直接叠在表面、找不到接合点 | 别问「有没有瑕疵」（陷阱 14），也别只跑内容描述——按部位逐条要**接触／投影／遮挡**的事实（问法模板与判据见「AI 生图的物理体检」节）；接触点裁切放大复核一次 |
| 19 | **媒体形态（静/动）被当成画面问题**——视觉模型给的「单帧静态插画／没有运动残影」是它的默认台词，它看不见文件层：外层 JPEG 底下可能整段拼着 MP4（Motion Photo／手机相册的「动态照片」），封面帧和视频帧同源，光看画面永远分不出 | **「动不动／有没有声音／是不是动图」是文件层问题**——直接查字节（`ftyp`／`moov`／`MotionPhoto="1"`，配方 `video-analysis/references/motion-photo.md`）。对方提到「动态」二字时问的就是形态，别拿画面层的「没有运动残影」当答案 |

## 通用对策（速记）

0. **验证配得上证据强度**——结构/线条类整图识别一次就稳（两家模型读法一致即可定论），别套裁切逐段复核；裁切放大是文字小字/水印/生僻字的专属手段。
1. **自家有档案先查档案**——但查档是为了核验与补细节，不是认图的必经步骤；画面指纹能直接指认的先报结果。
2. **模型点名当线索不当结论**——拿不准标「疑似」，不建档、不定论。
3. **关键信息（文字/角色/出处）找人多源验证**——B站搜索页、搜狗微信、百科；别搜一次就放弃。
4. **密集数字表漏格是常态**——空值标 `—`，绝不脑补填数。

完整版（格式与环境坑、多模型交叉验证、粤语方言字三条判据、超长截图分段、按列裁剪）→ `references/general-countermeasures.md`；几方读数打架时 → `references/multi-source-arbitration.md`

## 场景索引

| 你要做什么 | 打开 |
|:---|:---|
| 看陷阱的完整案例（1-9） | `references/traps-detail.md` |
| 把界面/表格的字读准（rapidocr 带坐标框） | `references/ui-ocr-workflow.md` |
| AI 生图质检（「总觉得哪里不对劲」） | `references/ai-image-physics-check.md` |
| 两图差分定位（水印／改图取证，脚本只读不写） | `references/image-diff-forensics.md` |
| 多源读数打架、终审 | `references/multi-source-arbitration.md` |
| 多模型／多问法交叉验证、环境与格式坑 | `references/general-countermeasures.md` |
| 视觉入口细节、QQ 收图、批量提示词 | `references/entrypoints.md` |
| DeepSeek Vision API 价格／参数／用法 | `references/deepseek-vision-api.md`（官方文档实录，含 bin/ds-vision 用法） |
| 客观描述问法模板（先答「这是什么」＋中性复核纪律） | `templates/objective-describe.md` |
| 视觉提示词库 v4/desc/struct/meme | `vision-prompts` skill（`bin/vprompt list|show`，唯一权威源；ds-vision `--prompt-name` 同源） |
| 表情包建档完整流程与案例（表情包识别陷阱节） | `internet-memes-reference` skill |
| 双生视界角色档案（点名核验） | `twin-vision-archive` skill |
| 统一入口（OCR ／ 差分） | `vtrap ocr <图>` ／ `vtrap diff A.jpg B.jpg`（= `scripts/vtrap.py`） |
